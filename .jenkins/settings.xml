<?xml version="1.0" encoding="UTF-8"?>
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 http://maven.apache.org/xsd/settings-1.0.0.xsd">

    <servers>
        <server>
            <id>tailoringexpert-releases</id>
            <username>${NEXUS_USERNAME}</username>
            <password>${NEXUS_PASSWORD}</password>
        </server>
    </servers>

    <profiles>
        <profile>
            <id>nexus</id>
            <properties>
                <tailoringexpert-releases.repository>${NEXUS_RELEASEURL}</tailoringexpert-releases.repository>
            </properties>
        </profile>
        <profile>
            <id>sonar</id>
            <properties>
				<sonar.host>${SONAR_HOSTURL}</sonar.host>
				<sonar.token>${SONAR_TOKEN}</sonar.token>
            </properties>
        </profile>
    </profiles>

    <activeProfiles>
        <activeProfile>nexus,sonar</activeProfile>
    </activeProfiles>
</settings>
